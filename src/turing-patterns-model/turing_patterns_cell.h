#ifndef TURING_PATTERNS_CELL
#define TURING_PATTERNS_CELL

class TuringPatternsCell {
    TuringPaternsCell(uint32_t row, _uint32_t col): _row(row), _col(col) {

    }

    double distance(const TuringPatternsCell& other) {

    }

 private:
    uint32_t _row;
    uint32_t _col;
};

#endif